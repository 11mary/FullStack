public class ArraydiffsyntaxID {
    public static void main(String[] args) {
        int array[]=new int[7];//size of array as we want

        array[0]=10; //values
        array[1]=30;
        array[2]=40;
        array[3]=50;
        array[4]=60;
        array[5]=70;
        array[6]=80;
        for(int i=0;i<array.length;i++)//syntax
        {
            System.out.println(array[i]);
        }
    }
}
